rootProject.name = "MultiVersion"

pluginManagement {
    repositories {
        // mavenLocal()
        mavenCentral()
        maven { url = uri("https://repo.blueberrymc.net/repository/maven-public/") }
    }
}
